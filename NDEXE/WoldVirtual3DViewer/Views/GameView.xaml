<UserControl x:Class="WoldVirtual3DViewer.Views.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra superior superpuesta o controles -->
        <Border Grid.Row="0" Background="#333333" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Salir / Desconectar" Command="{Binding GoBackCommand}" Width="150" Background="#FF4444"/>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Contenedor para Godot -->
        <WindowsFormsHost Grid.Row="1" x:Name="GodotHost">
            <wf:Panel x:Name="GamePanel" BackColor="Black"/>
        </WindowsFormsHost>
    </Grid>
</UserControl>
