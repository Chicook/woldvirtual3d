<UserControl x:Class="WoldVirtual3DViewer.Views.AvatarSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <Label Content="Selección de Avatar"
                   Style="{StaticResource TitleLabelStyle}"
                   Grid.Row="0"
                   Margin="0,0,0,30"/>

            <!-- Información -->
            <Border Grid.Row="1"
                    Background="#FF2D3748"
                    BorderBrush="#FF4A5568"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="0,0,0,20"
                    Padding="20">
                <TextBlock Text="¡Felicitaciones! Su PC ha sido registrado exitosamente. Ahora seleccione el avatar que usará en WoldVirtual3D. Para el desarrollo inicial, recomendamos usar el avatar 'Chica'."
                         Foreground="#FFCBD5E0"
                         FontSize="14"
                         TextWrapping="Wrap"/>
            </Border>

            <!-- Lista de avatares -->
            <Border Grid.Row="2"
                    Background="#FF1A202C"
                    BorderBrush="#FF4A5568"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="Avatares Disponibles"
                           Style="{StaticResource ModernLabelStyle}"
                           FontSize="16"
                           FontWeight="Bold"
                           Grid.Row="0"
                           Margin="0,0,0,20"/>

                    <!-- Lista de avatares -->
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding AvailableAvatars}"
                             SelectedItem="{Binding SelectedAvatar, Mode=TwoWay}"
                             Background="Transparent"
                             BorderBrush="Transparent"
                             Margin="0,0,0,20">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#FF2D3748"
                                        BorderBrush="#FF4A5568"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Margin="0,5,0,5"
                                        Padding="15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Placeholder para imagen del avatar -->
                                        <Border Grid.Column="0"
                                                Background="#FF63B3ED"
                                                CornerRadius="35"
                                                Width="70"
                                                Height="70"
                                                Margin="0,0,15,0">
                                            <TextBlock Text="{Binding Type, StringFormat={}{0}}"
                                                     FontSize="24"
                                                     FontWeight="Bold"
                                                     Foreground="White"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"/>
                                        </Border>

                                        <!-- Información del avatar -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}"
                                                     Foreground="White"
                                                     FontSize="16"
                                                     FontWeight="Bold"
                                                     Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding Description}"
                                                     Foreground="#FFCBD5E0"
                                                     FontSize="12"
                                                     TextWrapping="Wrap"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                <TextBlock Text="Estado:"
                                                         Foreground="#FFCBD5E0"
                                                         FontSize="11"
                                                         Margin="0,0,5,0"/>
                                                <TextBlock Text="{Binding Properties[status]}"
                                                         Foreground="{Binding Properties[status], Converter={StaticResource StatusToColorConverter}}"
                                                         FontSize="11"
                                                         FontWeight="Medium"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Indicador de selección -->
                                        <Border Grid.Column="2"
                                                Width="20"
                                                Height="20"
                                                CornerRadius="10"
                                                Background="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={StaticResource BoolToBrushConverter}}"
                                                BorderBrush="#FF63B3ED"
                                                BorderThickness="2"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="✓"
                                                     Foreground="White"
                                                     FontSize="12"
                                                     FontWeight="Bold"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Visibility="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <!-- Botón de continuar -->
                    <Button Grid.Row="2"
                            Content="Seleccionar Avatar y Continuar"
                            Style="{StaticResource ModernButtonStyle}"
                            Command="{Binding SelectAvatarCommand}"
                            CommandParameter="{Binding SelectedAvatar}"
                            Width="250"
                            Height="45"
                            HorizontalAlignment="Center"
                            Background="#FF48BB78"
                            IsEnabled="{Binding SelectedAvatar, Converter={StaticResource NullToBoolConverter}}"/>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
